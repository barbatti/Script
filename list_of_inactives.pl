#!/usr/bin/perl -w
# ========================================================================================================
#
# ========================================================================================================
use lib join('/',$ENV{"NX"},"CPAN") ;
use colib_perl;

print "Enter list of inactive atoms (e.g: 1-6,1-21,30): ";
$_=<STDIN>;
chomp;$_ =~ s/^\s*//;$_ =~ s/\s*$//;
$inact=$_;
$log ="inactive_list";
open(LOG,">$log");
@inactive=make_num_sequence($inact);
print LOG "inactive @inactive \n";
# ========================================================================================================
